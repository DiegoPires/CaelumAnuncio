<h1>Cadastro de anuncio</h1>

<p><%= notice %></p>
<p><%= alert %></p>

<% if user_signed_in? %>
  <%= render partial: "anuncios/form" %>
<% end %>

<br><br>

<table width="550" border=1>
  <tr>
    <th>Modelo</th>
    <th>Ano</th>
    <th>Valor</th>
    <th>Descricao</th>
    <th>Anunciante</th>
    <td width=120></th>
  </tr>
  <% @anuncios.each do |a| %>
    <tr>
      <td><%= a.modelo %></td>
      <td><%= a.ano %></td>
      <td><%= a.valor %></td>
      <td><%= a.descricao %></td>
      <td><%= a.anunciante.nome if a.anunciante %></td>
      <td align=center>
        <% if user_signed_in? && a.anunciante && a.anunciante = current_user %>
          <%= link_to "Editar", edit_anuncio_path(a) %> | 
          <%= link_to "Excluir", delete_anuncio_path(a), method: :delete, data: { confirm: "Mesmo?" } %>
        <% end %>
        <% unless a.aprovado %>
        <%= link_to "Aprovar", approve_anuncio_path(a), method: :put %>
        <% end %>
      </td>      
    </tr>
  <% end %>
</table>
